datasource db {
    url = "DATABASE_URL"
    user = "DATABASE_USER"
    password = "DATABASE_PASSWORD"
}

model User {
    id Int
    email String
    name String
}

model Post {
    title String
    content String
}